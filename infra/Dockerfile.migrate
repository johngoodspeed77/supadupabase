FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json tsconfig.base.json ./
COPY packages/db ./packages/db
RUN npm install --include=dev \
  && npm run build -w @supadupabase/db
CMD ["node", "packages/db/dist/migrate.js"]
